{
  "name": "facet-tag-navigator",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "esbuild main.ts --bundle --outfile=main.js --format=cjs --platform=node --external:obsidian --sourcemap=inline --watch",
    "build": "esbuild main.ts --bundle --outfile=main.js --format=cjs --platform=node --external:obsidian --minify",
    "deploy": "./deploy.sh"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "obsidian": "^1.6.0",
    "tslib": "^2.6.2",
    "typescript": "^5.5.4"
  }
}

